import 'package:flutter/material.dart';

class StructureItemCard extends StatelessWidget {
  const StructureItemCard({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
