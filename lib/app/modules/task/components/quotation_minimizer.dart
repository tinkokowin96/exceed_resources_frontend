import 'package:flutter/material.dart';

class QuotationMinimizer extends StatelessWidget {
  const QuotationMinimizer({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
