import 'package:flutter/material.dart';

class PhaseMinimizer extends StatelessWidget {
  const PhaseMinimizer({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
